name: Keep API warm

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the live API
        run: curl -s -o /dev/null -w "status: %{http_code}\n" https://civic-bridge-api.onrender.com/api/content
